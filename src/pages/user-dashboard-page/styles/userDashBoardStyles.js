export const userDashBoardStyles = () => {
    const mainContainer = 'bg-[var(--primary-transparent)] rounded-xl p-6 transition-colors duration-400';

    const titleSection = 'text-[1.75rem] text-[var(--primary-color)] font-medium mb-6 pb-1.5 border-b-2 border-[var(--accent-color)] transition-colors duration-400';

    return { mainContainer, titleSection };
};
